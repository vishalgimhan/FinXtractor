services:
  ollama:
    image: ollama/ollama
    container_name: finxtractor-ollama
    ports:
      - "11434:11434"
    volumes:
      - ollama-models:/root/.ollama
    restart: unless-stopped

volumes:
  ollama-models: